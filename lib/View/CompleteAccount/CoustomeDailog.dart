import 'package:flutter/cupertino.dart';

class CoustomDailog extends StatelessWidget {
  const CoustomDailog({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
